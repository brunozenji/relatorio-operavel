import { FiltraSeriePipe } from './filtra-serie.pipe';

describe('FiltraSeriePipe', () => {
  it('create an instance', () => {
    const pipe = new FiltraSeriePipe();
    expect(pipe).toBeTruthy();
  });
});
