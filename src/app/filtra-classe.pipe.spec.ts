import { FiltraClassePipe } from './filtra-classe.pipe';

describe('FiltraClassePipe', () => {
  it('create an instance', () => {
    const pipe = new FiltraClassePipe();
    expect(pipe).toBeTruthy();
  });
});
